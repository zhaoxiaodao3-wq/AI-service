# 阶段四：AI 长期向量记忆

## 原始需求

突破上下文窗口限制，实现跨会话、跨时间的 AI 长期记忆：

1. 每轮对话结束后，自动把“用户问题 + AI 回答”拼成记忆文本，切片/向量化后存入 Qdrant `memory_vectors`，绑定用户与会话。
2. 用户发起新提问时，自动检索该用户历史记忆，排除当前会话，按 TopK + 相似度阈值召回。
3. 双层记忆融合：最终 Prompt = 长期向量记忆 + 短期当前会话上下文 + 用户新问题；开启知识库时再叠加 RAG 片段。
4. 记忆降噪：相似度低于阈值的历史记忆不注入，避免回答错乱。
5. 提供学习文档。
