# 阶段九：RAG 增强（Rerank + 混合检索 + 引用溯源）

## 原始需求

1. 混合检索：向量检索 + 关键词检索，用 RRF 融合。
2. Rerank 精排：TopK 后再用 `BAAI/bge-reranker-v2-m3` 精排。
3. 引用溯源：回答关联文档片段，SSE `done` 携带 citations。
4. 前端展示引用片段（文档名 + chunk 序号）。
5. 学习文档。
