# 阶段十一补充：开源/免费模型检测接入

## 原始需求

企业级方案要求 Guard 使用开源或免费模型。本模块落地：

1. Guard Provider 可切换：`heuristic` / `llm_judge` / `prompt_guard`。
2. `llm_judge` 用免费 GLM 复核可疑输入。
3. `prompt_guard` 作为开源模型预留入口，支持 ONNX + transformers。
4. 提供模型下载脚本（hf-mirror）。
5. 模型/复核失败自动回退，不阻塞聊天。
