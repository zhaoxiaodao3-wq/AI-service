# 阶段一补充：前端打字机流式效果

## 原始需求

用户反馈：前端通过 SSE 连接，发送问题后内容其实已经很快返回，但页面仍停留在“加载中”动画，内容没有直接展示出来。

要求：

1. 第一个 `delta` 内容到达时，立即隐藏加载动画，直接展示 AI 内容。
2. 展示过程需要打字机动画：内容按小节奏逐段显示，而不是一次性全部出现。
3. 流结束时立即补全剩余内容，保证最终内容完整。
4. 不改变 SSE 协议与后端行为。
