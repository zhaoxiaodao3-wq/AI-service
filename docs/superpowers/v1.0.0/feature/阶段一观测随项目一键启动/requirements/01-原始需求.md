# 阶段一补充：观测栈随后端项目一键启动

## 原始需求

用户要求：后端项目启动时，Loki + Promtail + Grafana 观测栈**一起启动**，不需要再单独传 `-f docker-compose.observability.yml`。

1. 一条命令 `docker compose up -d --build` 启动 backend/postgres/qdrant + 观测栈。
2. 保持观测栈配置独立可维护。
3. 更新 README 与学习文档中的启动命令。
4. 如果镜像拉取受网络限制，配置与校验必须通过，并说明启动阻塞点。
