# Python 3.14 兼容：使用最低版本约束，pip 自动选择有 cp314 预编译轮子的最新版本
fastapi>=0.115
uvicorn[standard]>=0.34
litellm>=1.55
sqlalchemy>=2.0.36
psycopg[binary]>=3.2.3
pydantic>=2.11
pydantic-settings>=2.7
python-dotenv>=1.0.1
cryptography>=44
python-multipart>=0.0.20
# 与服务端镜像 v1.12.4 保持同大版本，避免版本不兼容警告
qdrant-client==1.12.1
pytest>=8.3
# httpx 由 litellm 自动决定版本，不显式锁定
