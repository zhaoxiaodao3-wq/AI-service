# AIGC对话\+RAG\+AI记忆项目 全阶段落地开发手册（零基础练手版）

## 项目核心定位（先明确，不跑偏）

这是一套**纯练手、重原理、重工程能力**的个人AIGC项目，不套开源成品、不依赖黑盒框架，主打4个核心能力打磨：

1. 多AI大模型统一对接（兼容官方Key \+ 中转Key，可自由切换）

2. 自研完整RAG链路（手动部署向量库、手动写检索逻辑，吃透原理）

3. 双层AI记忆实现（短期上下文记忆 \+ 长期向量记忆）

4. 完整AIGC工程架构（前端界面 \+ 后端服务 \+ 业务库 \+ 向量库 \+ 容器部署）

**最终成品**：AI网页对话平台，支持多模型切换、文档知识库问答、AI长期记忆、流式聊天、历史会话管理。

**技术栈固定（主流工业级，适配简历）**：

- 前端：Vue3 \+ TS \+ Element Plus

- 后端：Python FastAPI \+ LiteLLM（二次封装，不裸用）

- 业务数据库：PostgreSQL

- 向量数据库：Qdrant（Docker自建，核心学习点）

- 工具：LangChain（仅文档解析、切片，不套黑盒RAG）、SQLAlchemy、Docker Compose

---

## 整体开发总流程（由浅入深，绝对不踩坑）

阶段0：环境准备 \& 架构搭建

阶段1：基础对话能力（多模型对接 \+ 流式输出 \+ 短期记忆）

阶段2：数据持久化（用户/会话/消息全套数据库逻辑）

阶段3：自建向量库 \+ 完整RAG知识库（核心学习点）

阶段4：AI长期向量记忆功能（项目亮点）

阶段5：工程化完善（密钥管理、容错、统计、兼容中转/官方Key）

阶段6：容器化部署 \& 项目收尾（可上线、可写简历）

---

# 阶段0：环境搭建 \& 架构初始化（1天）

### 阶段目标

统一本地开发环境，搭建前后端基础架构，定义项目规范，为后续所有开发打底。

### 具体要做的事

1. **后端基础搭建**

    - 初始化FastAPI项目，配置分层架构（路由层、服务层、模型适配层、工具层）

    - 安装核心依赖：fastapi、uvicorn、litellm、sqlalchemy、pydantic、python\-dotenv、cryptography

    - 配置环境变量：区分开发/生产，预留LLM官方Key、中转Key、BaseURL配置项

2. **数据库环境搭建**

    - Docker部署PostgreSQL，创建项目专属数据库

    - 配置SQLAlchemy ORM，完成数据库连接测试

3. **向量库环境搭建**

    - Docker部署Qdrant向量数据库，测试连通性

    - 新建专属向量集合（Collection），用于存储文档向量、记忆向量

4. **前端基础搭建**

    - 初始化Vue3\+TS项目，配置路由、全局请求、UI组件库

    - 搭建基础页面框架：首页、聊天页、文档上传页

### 阶段交付成果

可运行的前后端基础项目、PostgreSQL\+Qdrant双数据库环境、统一配置文件

### 学习亮点

掌握AIGC项目标准分层架构、Docker基础部署、多环境配置规范

---

# 阶段1：基础AI对话 \+ 多模型对接 \+ 短期记忆（2天）

### 阶段目标

打通核心对话链路，实现**多模型自由切换、SSE流式打字效果、短期上下文记忆**，兼容官方Key/中转Key双模式。

### 具体要做的事

1. **封装多模型统一适配器（核心）**

    - 二次封装LiteLLM，不直接裸调用，自定义ModelAdapter层

    - 实现**一键切换模式**：官方直连 / 第三方中转（适配OpenAI、DeepSeek、Claude）

    - 统一所有模型的入参、出参、异常返回格式

2. **实现SSE流式对话接口**

    - 后端开发流式对话接口，处理模型流式分片数据

    - 前端对接SSE，实现AI打字实时输出效果

3. **实现短期记忆（会话上下文）**

    - 自研上下文拼接逻辑：每次对话自动携带当前会话历史消息

    - **手动实现Token截断策略**（核心练手点）：超过长度自动删除最早对话，避免上下文溢出

    - 支持多轮连续对话，模型可记住本轮会话全部内容

4. **异常容错处理**

    - 捕获Key失效、额度不足、模型超时、中转断流等异常，返回前端友好提示

### 阶段交付成果

可正常流式聊天、多模型切换、连续多轮对话、上下文自动截断的完整Demo

### 学习亮点

吃透大模型API对接原理、SSE流式通信机制、LLM上下文窗口限制与优化方案

---

# 阶段2：业务数据全持久化（1天）

### 阶段目标

实现所有业务数据落地数据库，完成用户、会话、消息、模型配置的完整CRUD，告别临时内存数据。

### 具体要做的事

1. **设计核心数据库表结构**

    - 用户表：账号、密码、创建时间

    - AI模型配置表：模型名称、厂商、启用状态、APIKey（加密存储）、BaseURL、权重

    - 会话表：会话ID、用户ID、所选模型、会话标题、创建时间

    - 消息表：归属会话、角色、内容、Token消耗、创建时间

2. **实现全套业务接口**

    - 会话：新建、删除、切换、重命名、查询历史会话

    - 消息：保存每轮对话、查询会话历史消息

    - 模型：动态加载可用模型，前端下拉切换

3. **安全处理**

    - 对数据库中所有AI的APIKey进行加密存储，禁止明文保存

### 阶段交付成果

所有对话数据永久保存，刷新页面不丢失，支持多会话管理、动态配置模型

### 学习亮点

掌握AIGC业务数据建模、敏感数据加密、后端CRUD工程化开发

---

# 阶段3：自建Qdrant向量库 \+ 完整RAG知识库（3天，核心重点）

### 阶段目标

**全程手写RAG核心逻辑**，不依赖框架黑盒，吃透文档问答全链路，亲手实现向量数据库增删改查。

### 具体要做的事

1. **文件上传与解析**

    - 前端实现PDF/TXT/MD文件上传功能

    - 后端接收文件，保存文件元信息到PostgreSQL

    - 利用LangChain工具解析文件，提取纯文本内容（仅用工具，逻辑自研）

2. **文本切片Chunk（关键调参点）**

    - 手动设置切片大小、重叠度，实现长文本分割

    - 理解切片逻辑：解决Embedding长度限制、提升检索精准度

3. **Embedding向量化 \+ 存入向量库**

    - 调用Embedding模型API，将文本切片转为向量

    - 手动编写Qdrant入库逻辑：存储向量、原文、文档ID、用户ID

4. **自研相似度检索逻辑**

    - 用户提问 → 问题向量化 → Qdrant相似度检索

    - 手动设置TopK召回数量、相似度阈值，过滤无效片段

5. **RAG Prompt组装 \& 问答**

    - 手动拼接系统提示词 \+ 检索到的知识库片段 \+ 用户问题

    - 调用大模型，实现**基于上传文档的专属问答**

### 阶段交付成果

完整可用的RAG知识库功能，上传文档后AI可精准基于文档内容回答，全程可追溯、可调试

### 学习亮点

吃透RAG核心五步法：文档解析→切片→向量化→检索→生成，掌握向量数据库核心原理与调参技巧（简历核心亮点）

---

# 阶段4：AI长期向量记忆功能（2天，项目加分亮点）

### 阶段目标

突破大模型上下文窗口限制，实现**跨会话、跨时间的AI长期记忆**，区别于普通短期对话记忆。

### 具体要做的事

1. **对话记忆向量化入库**

    - 每轮对话结束后，自动拼接用户问题\+AI回答，生成记忆文本

    - 对记忆文本切片、向量化，存入Qdrant向量库，绑定唯一用户ID

2. **提问自动召回历史记忆**

    - 用户发起新提问时，优先检索该用户所有历史对话向量

    - 筛选高相似度记忆片段，注入Prompt

3. **双层记忆融合逻辑**

    - 最终Prompt = 长期向量记忆片段 \+ 短期当前会话上下文 \+ 用户新问题

    - 解决长对话遗忘、跨会话失忆问题

4. **记忆降噪优化**

    - 设置相似度阈值，过滤无关记忆，避免AI回答错乱

### 阶段交付成果

AI拥有长期记忆，隔时、换会话提问，仍能回忆起历史对话内容，远超普通对话项目

### 学习亮点

理解长短记忆差异、突破LLM上下文限制的核心方案，掌握AI个性化记忆实现原理（面试高频考点）

---

# 阶段5：工程化完善 \& 双模式兼容（2天）

### 阶段目标

优化项目稳定性、安全性、可用性，完美兼容**中转Key \+ 官方Key**，架构无依赖、可随时切换。

### 具体要做的事

1. **模型配置优化**

    - 实现配置化切换：无需改代码，后台配置即可切换官方直连/中转代理

    - 适配多厂商模型：DeepSeek、通义千问、Claude、GPT

2. **安全与容错优化**

    - 完善APIKey加密存储、接口限流、请求超时重试机制

    - 解决中转SSE断流、返回异常等兼容问题

3. **数据统计功能**

    - 统计每次对话Token消耗、模型调用次数、成功率

4. **日志系统**

    - 记录模型调用日志、错误日志、用户操作日志，方便排查问题

### 阶段交付成果

工程化完整的AIGC项目，稳定、安全、可配置、无黑盒依赖

---

# 阶段6：Docker容器化部署 \& 项目收尾（1天）

### 阶段目标

实现项目一键部署，整理项目文档、简历亮点，完成最终成品。

### 具体要做的事

1. **编写Docker Compose配置**

    - 编排四大服务：FastAPI后端、PostgreSQL、Qdrant、前端静态资源

    - 实现**一键启动整套项目**

2. **项目收尾**

    - 修复全部bug，优化前端交互、流式体验

    - 编写项目README：架构说明、部署教程、功能介绍

3. **简历亮点梳理**

    - 提炼架构设计、RAG自研、双层记忆、多模型适配、工程化优化等核心亮点

---

## 项目最终完整能力清单（可直接写简历）

1. 基于FastAPI自研AIGC后端架构，封装多模型统一适配器，兼容官方API与第三方中转协议，支持多厂商大模型一键切换

2. 自研双层AI记忆体系：手动实现会话上下文短期截断记忆 \+ 基于Qdrant向量检索的跨会话长期记忆，突破LLM上下文窗口限制

3. 从零搭建完整RAG知识库系统，独立部署向量数据库，自研文档解析、切片、向量化、相似度检索、Prompt组装全链路逻辑

4. 基于SSE协议实现AI流式对话输出，优化用户聊天交互体验

5. 采用PostgreSQL存储业务数据，实现用户、会话、模型配置、对话消息全量持久化，敏感密钥加密存储

6. 完成项目容器化编排部署，完善异常容错、限流、日志统计等工程化能力

## 核心避坑总结（全程适用）

- 中转Key仅用于开发调试，架构完全解耦，不依赖第三方黑盒服务

- RAG、记忆核心逻辑全部手写，不套框架封装，吃透底层原理

- 业务数据存PostgreSQL，向量数据存Qdrant，二者分工明确不混用

- 所有Key不硬编码、不明文存储，严格遵守AIGC项目安全规范

> （注：部分内容可能由 AI 生成）
